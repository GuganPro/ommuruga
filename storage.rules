rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access to product images, but only allow authenticated
    // users to upload them.
    match /products/{imageId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
